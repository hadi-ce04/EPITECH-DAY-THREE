def count(text):
    lower = text.lower()
    targets = ["cat", "garden", "mice"]
    total = 0
    for word in targets:
        total += lower.count(word) + lower.count(word[::-1])
    return total

a = "the CataCat attaCk a Cat"
b = "thE Cat's tactic wAS tO surpRISE thE mIce iN tHE gArdeN"

print(count(a))
print(count(b))
